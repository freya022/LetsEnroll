services:
  db:
    image: postgres:17
    restart: unless-stopped
    env_file: ./config/.env
  bot:
    image: commandinator:latest
    restart: unless-stopped
    volumes:
      - type: bind
        source: ./config
        target: /app/config
