package dev.freya02.commandinator.api.bot

import org.springframework.stereotype.Component

@Component
class BotClient(

) {

    fun isInGuild(guildId: Long, userId: Long): Boolean {
        TODO()
    }
}